# PyWorkflow Dashboard Backend Configuration
# Copy this file to .env and modify as needed

# Path to pyworkflow.config.yaml (optional)
# If set, pyworkflow will be configured from this YAML file
# If not set, pyworkflow will auto-discover pyworkflow.config.yaml in the current directory
# DASHBOARD_PYWORKFLOW_CONFIG_PATH=/path/to/pyworkflow.config.yaml

# Storage configuration (fallback if pyworkflow config not set)
# DASHBOARD_STORAGE_TYPE=file
# DASHBOARD_STORAGE_PATH=./pyworkflow_data

# Server configuration
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=8585

# CORS origins (comma-separated for multiple origins)
# DASHBOARD_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Debug mode
# DASHBOARD_DEBUG=false
